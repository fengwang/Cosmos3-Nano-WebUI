/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standalone output → a slim, self-contained server bundle for the CPU webui image.
  output: "standalone",
};

export default nextConfig;
