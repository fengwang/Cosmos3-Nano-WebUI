"""Cosmos3-Nano Serving API — runtime foundation (Session 1).

Package root `app` (rooted at ``api/app/``). Engine-free in S1: health + schema
stub only; real engine planes arrive in S2+ (`docs/project_contract.md`).
"""
