# Risk Register - GPU Release Readiness and Upstream Quant Contribution

Date: 2026-07-08

Status values are blueprint-time. Sessions update rows with evidence as they
close. Where a row advances or closes a Phase-1 risk, the archived ID is
noted; see `docs/archive/phase-1/risk_register.md` for that history.

| ID | Risk | Probability | Impact | Owner Session | Mitigation / Gate | Status |
|---|---|---:|---:|---|---|---|
| R-01 | The public base image (`vllm/vllm-openai:<version>`) does not support sm_120/Blackwell, so the from-source build still cannot produce a working image. | Medium | High | GPU-S1 | Confirm the chosen base tag's CUDA/torch build supports sm_120 before committing to it; record the check. Advances archived R-13. | **Closed (2026-07-09).** `vllm/vllm-openai:v0.24.0` confirmed sm_120-capable via a live bf16 matmul probe on the RTX 5090; `deploy/vllm-omni.Dockerfile` rebuilt on that base builds and serves T2I for FP8 and NVFP4. Retires the build half of archived `R-13`. See `docs/evidence_map.md`. |
| R-02 | Fixing the `wfen/*` Hugging Face repos changes their revision hashes and breaks any early adopter who already cloned or downloaded the beta-GO revision. | Low | Medium | GPU-S2 | Document the revision change in the model card / release notes; this is a one-time public-beta cost, not a recurring one. | **Accepted, documented (2026-07-09).** Revisions changed: FP8 `4e181f99…` → `9bf5d6ae1646…`; NVFP4 `b5c9332e…` → `5514c42b9759…`. Recorded here rather than editing either HF repo's own README/model card (kept the external blast radius to exactly index+LFS, per owner decision during `GPU-S2` brainstorming). One-time cost accepted as scoped; no further action. |
| R-03 | The re-pin sweep misses a reference (a file, a comment, an eval case) after the Hugging Face revisions change, leaving a stale pin somewhere in the repo. | Medium | Medium | GPU-S2 | Run an exact-match `rg` sweep for the old revision SHAs across the whole repo, not only the four named files, before closing the session. | **Closed (2026-07-09), after the risk materialized once during this session.** The session's own first sweep (`rg -n "4e181f99\|b5c9332e" .`) found and fixed `README.md` (amendment `GPU-S2-A2`) but, because `rg` skips dotfiles by default, missed `.env.example`'s live "Downloading the pinned public checkpoints" guidance — caught only by a fresh-context adversarial verifier independently re-running the check with `--hidden`, which returned a FAIL verdict on exactly this gap. Fixed as amendment `GPU-S2-A3`: `.env.example` corrected, and the sweep command itself (in this contract's `deterministic_checks` and every `docs/session_2/**` copy) now requires `--hidden --glob '!.git'`. Re-swept clean with the corrected command — see `docs/session_2/adversarial_verification.md`. `docs/archive/phase-1/**` matches remain out of bounds by contract and untouched. |
| R-04 | Migrating small files out of LFS accidentally also de-tracks a large weight file from LFS, bloating repository history. | Low | High | GPU-S2 | Apply the LFS rule by explicit file-size/type check, not a blanket `.gitattributes` rewrite; diff the LFS-tracked file list before and after. | **Closed (2026-07-09).** For both repos, `git diff --stat <pre-fix HEAD> <new HEAD>` against every large/oversized file (`transformer/*.safetensors`\|`model.safetensors`, `vae/`, `vision_encoder/`, `sound_tokenizer/` weights, `modelopt_state.pt`, `text_tokenizer/tokenizer.json`) produced empty output — byte-identical, not merely same-OID. No large file was de-LFS'd. |
| R-05 | The `GPU-S1` image builds and the `GPU-S2` checkpoints load individually, but T2I/T2V still fails together for a reason other than the already-known drift D1. | Medium | High | GPU-S3 | Run the joint validation as its own session with full evidence fields, not as an assumed consequence of GPU-S1 and GPU-S2 each passing alone. Advances archived R-03 and R-13. | **Closed (2026-07-09).** Joint validation ran as its own session: fresh `hf download` at the `GPU-S2` revisions through the unmodified `GPU-S1` image generated valid FP8 and NVFP4 T2I (direct and full-stack) with no manual workaround, and a best-effort NVFP4 T2V smoke also passed on the first attempt. No new failure mode beyond the already-known D1 (in-process oracle only, unaffected here) surfaced. See `docs/evidence_map.md`. Advances archived R-03 and R-13 as planned. |
| R-06 | Upstream `vllm-project/vllm-omni` `main` has drifted enough that the isolated quant commits do not rebase cleanly, or upstream already has overlapping/conflicting quant code. | Medium | Medium | GPU-S4 | Check upstream state before isolating any code; resolve conflicts only in the isolated quant-loader surface, escalating anything Cosmos3-specific to the owner. | **Closed for `GPU-S4` (2026-07-09).** Upstream overlap exists but is bounded: generic ModelOpt detection and the NVFP4 W4A4 `weight_scale` NaN-clamp hunk are already upstream, so `GPU-S4` deliberately did not re-import those pieces. The branch is based on `upstream/main` `a5db2d839a0a20ddb0090faa5bb233280601e5eb`, contains two DCO-signed commits through head `f7e024ddc9965622ebcfdb919e8ccb46b4232074`, and `git rebase upstream/main` reports it is up to date. |
| R-07 | The quant contribution cannot be fully decoupled from Cosmos3-specific code without breaking a guard or adapter the fork depends on. | Medium | Medium | GPU-S4, GPU-S5 | Isolate model-agnostic files first (`GPU-S4`); if a dependency cannot be cut cleanly, stop and record the coupling rather than shipping a partially-decoupled PR. | **Closed for `GPU-S4`; monitor in `GPU-S5` (2026-07-09).** The isolated branch's diff is limited to model-agnostic quant-loader files, model-agnostic config wiring, and narrow tests. A branch-diff sweep for Cosmos3 names, session-doc references, private paths, `HF_TOKEN`, and `private` returned zero matches after cleanup and after review fixes. `GPU-S5` still owns PR precheck, upstream CI, and any maintainer-requested narrowing. |
| R-08 | DCO sign-off, upstream CLA, or maintainer review stalls or blocks the pull request indefinitely. | Medium | Low | GPU-S5 | Confirm DCO/CLA requirements and run `precheck-pr` before submission; a stalled PR is a known, accepted outcome, not a release blocker for this repository. | **Mitigated for submission (2026-07-09).** PR #5000 is open, every branch commit is DCO-signed, and GitHub checks are green (`DCO`, `pre-commit`, wheel builds for Python 3.11/3.12, Read the Docs). Maintainer review remains out of `GPU-S5` scope and is a post-Phase-2 follow-up, not a release blocker for this repository. |
| R-09 | The RTX 5090 host is unavailable or contended during `GPU-S1`/`GPU-S3`, blocking build or validation work. | Low | Medium | GPU-S1, GPU-S3 | Treat GPU host availability as a session precondition; record the conflict and reschedule rather than substituting an unverified environment. | **Closed for `GPU-S1` and `GPU-S3` (2026-07-09).** Host confirmed available and idle (0% util, 15 MiB used) before `GPU-S1`'s build/serve/T2I work. `GPU-S3` independently re-verified at its own execution time (same idle reading, re-checked via `nvidia-smi` rather than reused from `GPU-S1`'s observation) before starting; no contention encountered in either session. |
| R-10 | The shipped `deploy/vllm-omni.Dockerfile` default (guardrails on) has never completed a generation end to end; it is only proven to fail closed without the gated `nvidia/Cosmos-1.0-Guardrail` model / `HF_TOKEN`. All `GPU-S1` T2I evidence used an explicit `--no-guardrails` override. | Medium | Low | GPU-S3 | Provision gated guardrail model access and `HF_TOKEN` in a session with GPU access, then re-run the T2I smoke with guardrails on to close this row; until then, treat the guardrails-on path as unverified rather than assumed-working. Not a `GPU-S1` regression — the same flag was already required for Phase-1's own proven GPU gate. | Open — surfaced by `GPU-S1`'s sharded review (F2) and adversarial verification, 2026-07-09. |
| R-11 | This repo's own `.env` pins `COSMOS3_FP8_DIR`/`COSMOS3_NVFP4_DIR` to a local, stale, pre-fix checkpoint directory (`/data/models/Cosmos3-Nano-*-Blockwise`) for developer convenience, and that pin silently outranks `deploy/docker-compose.{fp8,nvfp4}.yml`'s own repo-relative default. `session_3_contract.yaml`'s and `docs/session_3.md`'s own literal `deterministic_checks` block (`docker compose -f deploy/docker-compose.fp8.yml up -d`) — if run bare, exactly as documented, by a future session or an operator reproducing this session's result — silently mounts that stale directory. `GPU-S3`'s own probes only avoid this because `docs/session_3/probes/compose_lifecycle.py` explicitly overrides the env var and preflight-checks the resolution before every `bring_up()` call; nothing protects a bare reproduction of the contract's own documented command. | Medium | Medium | Unowned (surfaced by `GPU-S3` adversarial verification round 2) | Either the deterministic-checks documentation is updated (out of `GPU-S3`'s blast radius to do unilaterally — `session_3_contract.yaml`/`session_3.md` are not files this session may edit) to name the required env override explicitly, or a future session's own `.env.example`/onboarding guidance is changed so the local-dev convenience default doesn't silently defeat the fresh-verification discipline for anyone who doesn't go through session-specific tooling. | **Partially closed (2026-07-09, amendment `GPU-S3-A1`).** Owner explicitly authorized editing `.env.example` (outside the original blast radius) to add an explicit warning at `COSMOS3_FP8_DIR`/`COSMOS3_NVFP4_DIR` about this exact failure mode and the `docker compose config` check to run before trusting any `up -d` as validating a fresh checkpoint. This helps any future operator who starts from a fresh `.env.example` copy, but does **not** change `session_3_contract.yaml`'s/`docs/session_3.md`'s own literal `deterministic_checks` text (still outside blast radius) and does not retroactively warn an operator whose `.env` was already customized before reading this. Residual gap: the contract's own documented command is still silently wrong on this host's actual `.env` today. |
