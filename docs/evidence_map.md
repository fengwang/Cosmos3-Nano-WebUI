# Evidence Map - GPU Release Readiness and Upstream Quant Contribution

Date: 2026-07-08

Rules:

- Claims without evidence are marked speculative.
- Speculative claims cannot become MUST-level requirements.
- Owner decisions can become contract constraints, but technical feasibility
  still needs verification by the owning session.
- This repository is public (post Phase-1 GO). Entries must not cite private
  hosts, private absolute paths, or private-only artifacts; use the archived
  Phase-1 docs (`docs/archive/phase-1/**`) as historical source, not as a
  channel for reintroducing private evidence.

| Claim | Evidence | Source | Confidence | Gap / Risk |
|---|---|---|---|---|
| `docs/archive/phase-1/` now holds the full Phase-1 contract pack, handoffs, session artifacts, and `mig_s*` eval-corpus entries. | `git status` recorded 165 renamed paths after the archive move; top-level `docs/` retains only `model_setup.md`, `release_checklist.md`, `agent_workflow/**`, and the fresh Phase-2 pack. | Local repository check, 2026-07-08 | High | None; mechanical move, verified by git status. |
| The public `deploy/vllm-omni.Dockerfile` fails at build step 3/3: `pip install --break-system-packages` is unsupported by the base image's pip 22.0 (Ubuntu 22.04). | Build attempt recorded during the post-GO GPU gate. | `docs/archive/phase-1/risk_register.md` R-13; `docs/release_checklist.md` §6 | High | The rework is unproven until `GPU-S1` runs the new recipe. |
| The `-runtime` CUDA base lacks a build toolchain, and the Dockerfile's `CMD` invokes the wrong entrypoint. | Same build attempt. | `docs/archive/phase-1/risk_register.md` R-13; `docs/release_checklist.md` §6 | High | The exact replacement base tag is not yet chosen or verified for sm_120. |
| The fork's own Docker pattern is `FROM vllm/vllm-openai:v0.24.0` plus `uv pip install .`, not a from-scratch CUDA-devel build. | Fork's `docker/Dockerfile.cuda`. | `docs/archive/phase-1/next_phase_handoff.md` | High | Whether that base tag (or whichever tag `GPU-S1` chooses) supports sm_120/Blackwell is unverified until `GPU-S1` runs it. Speculative until then. |
| The confirmed vLLM-Omni serve entrypoint is `vllm serve <checkpoint-dir> --omni --host 0.0.0.0 --port 8000 …`. | Fork's `recipes/cosmos3/Cosmos3-Nano.md`, confirmed during the post-GO GPU gate. | `docs/release_checklist.md` §6 | High | None; already exercised once against a prebuilt image. |
| Both HF checkpoints ship a stale top-level `model.safetensors.index.json` referencing 7 non-existent shards; the real weight is a single consolidated file (drift D1). | GPU gate run, 2026-07-08. | `docs/archive/phase-1/risk_register.md` R-03; `docs/archive/phase-1/session_4/drift_report.md` | High | The root-cause fix (removing the index at the HF-repo level) is unproven until `GPU-S2` runs it. |
| Small config/tokenizer files on both HF repos are LFS-tracked, so a plain `git clone` leaves them as unresolved pointers; `hf download` avoids this. | GPU gate notes, 2026-07-08. | `docs/release_checklist.md` "GPU gate exercised" section | High | The LFS-tracking fix itself is unproven until `GPU-S2` runs it. |
| The pinned vLLM-Omni fork commit is `697035018b70cef76b974a909d23371a9984c3f2`. | `git ls-remote` against the public fork branch/tag. | `docs/archive/phase-1/evidence_map.md` | High | Stable as long as `GPU-S1` continues to pin by this commit; `GPU-S4` may add commits on top for the upstream contribution but must not change this pin. |
| T2I generation was proven end to end for FP8 and NVFP4 on an RTX 5090, but using a prebuilt local image (`vllm-omni-local:c89089a4`), not a build from the pinned commit via the public Dockerfile. | Post-GO GPU gate, 2026-07-08. | `docs/release_checklist.md` "GPU gate exercised" section | High | This is exactly the gap `GPU-S1` and `GPU-S3` close. |
| HF write access to `wfen/*` and the RTX 5090 host are available prerequisites for this work. | Owner decision recorded while scoping this phase. | `docs/archive/phase-1/next_phase_handoff.md` | High | Owner-stated; not independently re-verified in this blueprint pass. |
| Upstream `vllm-project/vllm-omni` `main` at `a5db2d839a0a20ddb0090faa5bb233280601e5eb` partially overlaps the fork slice but still lacks the native FP8/NVFP4 blockwise sidecar loaders and quant config modules needed for the contribution. | `GPU-S4` fetched upstream before any isolation commit, searched `vllm_omni/quantization`, checkpoint adapters, and `patch.py`, and recorded the finding. Upstream already has generic ModelOpt FP8/NVFP4 config detection plus the NVFP4 W4A4 `weight_scale` NaN-clamp hunk, but does not have `fp8_blockwise_w8a16.py`, `nvfp4_blockwise.py`, or `modelopt_native*.py`. The final `GPU-S4` branch was `fengwang/vllm-omni@gpu-s4-quant-loader-isolation` head `f7e024ddc9965622ebcfdb919e8ccb46b4232074`, based on that upstream main, with two DCO-signed commits. | `docs/session_4/upstream_state.md`, `docs/session_4/checks.md`, external fork branch `gpu-s4-quant-loader-isolation` | High | Superseded for submission by `GPU-S5`, which rebased the same branch onto current upstream and opened PR #5000. |
| The fork's `precheck-pr` skill and `CONTRIBUTING.md` guidance were available in the `fengwang/vllm-omni` fork checkout and were applied before PR submission. | `GPU-S5` verified `.claude/skills/precheck-pr/SKILL.md`, `references/checklists.md`, `references/code-quality.md`, `CONTRIBUTING.md`, `.github/workflows/pre-commit.yml`, `.github/workflows/build_wheel.yml`, and `.pre-commit-config.yaml`; quick and full manual reports saved with zero blockers and two warnings. | `docs/session_5/precheck_quick.md`, `docs/session_5/precheck_full.md`, `docs/session_5/checks.md` | High | The skill is documentation/checklist based rather than an executable script; `GPU-S5` reconstructed quick and full modes manually and recorded the evidence. |
| The upstream quant contribution PR is open, DCO-signed, and green on GitHub checks. | `GPU-S5` cleanly rebased the fork branch onto upstream `main` `ca0ae7269ca3e9487645cf66088fdfc338951da9`, fixed local `pre-commit` findings in a DCO-signed cleanup commit, pushed `fengwang:gpu-s4-quant-loader-isolation` at `a33df880b83da06087ca4ca13eb061a567cbfe36`, recorded explicit owner approval at `2026-07-09T19:40:49Z`, and opened `https://github.com/vllm-project/vllm-omni/pull/5000`. GitHub checks are green: `DCO`, `pre-commit`, `build (3.11)`, `build (3.12)`, and `docs/readthedocs.org:vllm-omni` all `SUCCESS`. | `docs/session_5/pr_record.md`, `docs/session_5/checks.md`, PR #5000 | High | Maintainer review is intentionally out of `GPU-S5` scope and remains a post-Phase-2 follow-up, not a release blocker here. |
| A local checkout of the `fengwang/vllm-omni` fork was present as a sibling directory in this development environment, at HEAD equal to the pinned commit `697035018b70cef76b974a909d23371a9984c3f2`. | Confirmed during the adversarial review pass, 2026-07-08. | Local repository check, 2026-07-08 | High | Environment-specific; a later session must not assume this checkout exists elsewhere, and must still verify state against the public GitHub remote rather than relying solely on a local copy. Superseded in practice by `GPU-S1`: no such sibling checkout existed in the `GPU-S1` execution environment, and the fork's `docker/Dockerfile.cuda` / `recipes/cosmos3/Cosmos3-Nano.md` were instead fetched directly from `raw.githubusercontent.com` at the pinned commit. |
| `vllm/vllm-openai:v0.24.0` (the base image tag the fork's `docker/Dockerfile.cuda` pins) supports sm_120/Blackwell at runtime, not just at pull/compile time, on the target RTX 5090. | `docker run --gpus all --entrypoint python3 vllm/vllm-openai:v0.24.0 -c '...'` reported `torch 2.11.0+cu130`, `vllm 0.24.0`, `capability (12, 0)`, and completed a real bf16 4096×4096 matmul on the GPU (`sum=144384.0`). | `GPU-S1` execution, 2026-07-08/09 | High | Closes `R-01`. The pull itself was near-free (~2s) since the tag shares nearly all layers with already-cached `vllm-openai` tags in this environment; that reuse is environment-specific and not itself evidence about network cost elsewhere. |
| `deploy/vllm-omni.Dockerfile`, rebuilt `FROM vllm/vllm-openai:v0.24.0` with the fork installed via `uv pip install "git+https://…@697035…"` (immutable SHA) and `ENTRYPOINT []` + a corrected `CMD`, builds successfully from public inputs only. | `docker compose --env-file .env -f deploy/docker-compose.fp8.yml build vllm-omni` exited 0 in ~48s (first attempt) / <2s (cached re-build after a CMD-only edit). `docker history`/`RootFS.Layers` comparison: the new image's only 2 non-base layers (apt `git`, `uv pip install`) share zero layer IDs with the locally cached `vllm/vllm-omni:cosmos3` (27 GB) prebuilt; the 5 layers that *are* shared with `cosmos3` are already present in the untouched `vllm-openai:v0.24.0` base itself (base-OS ancestry, not anything this build added). | `GPU-S1` execution, 2026-07-08/09 | High | Two build-time findings baked into the Dockerfile: (1) `uv pip install` needs `--system` (no venv is active in the base image); (2) the fork's build backend (`setuptools_scm`/`vcs_versioning`) can't parse a version from the nearest reachable tag (`cosmos3-nano-webui-mig-s2`) at this pinned commit, so `SETUPTOOLS_SCM_PRETEND_VERSION=0.0.0` is set to route around that fork-side build-tooling bug without patching the fork. |
| The rebuilt image serves `/v1/models` and generates a valid T2I artifact on the RTX 5090 for **both** FP8 and NVFP4, using the already-known local checkpoint workaround (exceeds this session's "at least one" bar). | `docker compose --env-file .env -f deploy/docker-compose.fp8.yml up -d vllm-omni` → `/v1/models` HTTP 200 → `POST /v1/images/generations` (1024×1024, 50 steps, seed 42) → HTTP 200, decoded to a valid 1024×1024 RGB PNG (FP8: 3,147,861 bytes, sha256 `3fd20c32…`; NVFP4: 3,147,861 bytes, sha256 `8316faf0…`), visually confirmed as the requested "red sports car at golden hour" scene for both. Repeated identically for the NVFP4 stack (`docker-compose.nvfp4.yml`) against the same built image. Hardware: RTX 5090, sm_120, driver reporting CUDA 13.0 via the container; vLLM-Omni commit `697035018b70…`; FP8 checkpoint `wfen/Cosmos3-Nano-FP8-Blockwise` (pre-`GPU-S2` revision `4e181f99…`, local stale-index workaround already applied on disk); NVFP4 checkpoint `wfen/Cosmos3-Nano-NVFP4-Blockwise` (pre-`GPU-S2` revision `b5c9332e…`, same workaround applied). | `GPU-S1` execution, 2026-07-08/09 | High | Guardrails were disabled (`--no-guardrails`) only via an untracked Compose override for this smoke test — the shipped Dockerfile `CMD` keeps guardrails on by default, since `CosmosSafetyChecker` hard-fails startup without the gated `nvidia/Cosmos-1.0-Guardrail` model / `HF_TOKEN`, neither provisioned in this session. Artifacts themselves are not committed (INV-1/forbidden-files: generated media). |
| `deploy/docker-compose.local-image.yml` (the local-image stopgap) is dispositioned: deleted, not kept as a documented convenience. | `git status --short` after removal shows the file neither tracked nor untracked; `rm deploy/docker-compose.local-image.yml` in `GPU-S1`. | Owner decision recorded in `docs/session_1/brainstorming.md`, executed 2026-07-09 | High | Closes PRD Owner Decision 8 for this repository. The file was never committed, so no existing clone references it. |
| Both `wfen/*` checkpoint repos' stale top-level `model.safetensors.index.json` is removed, and small config/tokenizer/example files are regular Git blobs (not LFS pointers) at the new revisions `wfen/Cosmos3-Nano-FP8-Blockwise@9bf5d6ae164688487bdb71947ccc6ebe70d12900` and `wfen/Cosmos3-Nano-NVFP4-Blockwise@5514c42b9759739f545e0d0dee453db8d8525fbc`. | Two working-clone commits per repo (index removal + orphan/LFS-tracking restoration), pushed after an explicit per-repo owner go-ahead; independently re-verified via a cache-isolated fresh `git clone` (`GIT_LFS_SKIP_SMUDGE`-equivalent `lfs.fetchexclude` for large files only) and `hf download --exclude "*.safetensors" ...` against the new revisions — both tools show real content for every small file and no top-level index. | `GPU-S2` execution, 2026-07-09 | High | Closes archived Phase-1 R-03. `GPU-S3` must use these revisions, not the pre-fix `4e181f99…`/`b5c9332e…` ones. |
| The large weight files in both `wfen/*` repos (`transformer/*.safetensors`\|`model.safetensors`, `vae/diffusion_pytorch_model.safetensors`, `vision_encoder/model.safetensors`, `sound_tokenizer/diffusion_pytorch_model.safetensors`, `transformer/modelopt_state.pt`) are byte-for-byte unchanged by the `GPU-S2` fix — no large file was accidentally de-LFS'd. | `git diff --stat <pre-fix-HEAD> <new-HEAD> -- <each large file path>` produced empty output for every one, for both repos, checked before either push. | `GPU-S2` execution, 2026-07-09 | High | Directly closes R-04. |
| `wfen/Cosmos3-Nano-FP8-Blockwise`'s `BIAS.md`/`EXPLAINABILITY.md`/`PRIVACY.md`/`SAFETY.md` (identical content in NVFP4) and 32 further NVFP4-side files (`config.json`, `model_index.json`, every tokenizer file, etc.) previously checked out as raw LFS-pointer text on a normal clone — a third, previously undocumented packaging bug, unrelated to `.gitattributes` pattern matching (no rule has ever matched `.md`; NVFP4's text-extension rules had already been removed pre-session without a renormalize). | Direct LFS object fetch + manual smudge recovered real content for all of them at the pre-fix HEAD; restored via the same mechanism and pushed as part of the `GPU-S2` commits above; re-confirmed present via the independent fresh-clone verification. | `GPU-S2` execution, 2026-07-09 (owner-approved amendment `GPU-S2-A1`) | High | `_s2_postfix.md`/`_s2_rerun.md`/`_s2_verify.md` (FP8) and `transformer/producer_provenance.json` (NVFP4) have the identical corruption and are deliberately left untouched — dev-scratch, Owner Decision 3. |
| A genuinely fresh `hf download` of both `wfen/*` repos at the `GPU-S2` revisions, through the unmodified `GPU-S1` image (`cosmos3-nano-vllm-omni:local`, confirmed current via `git diff --quiet` against `deploy/vllm-omni.Dockerfile`), proves FP8 **and** NVFP4 T2I end to end — **both direct (bypassing the api) and full-stack (`X-API-Key` → job → artifact)** — with no manual index-removal or LFS-pointer workaround. | Fresh download: FP8 222 files/22.4 GiB, NVFP4 116 files/24.8 GiB, both independently spot-checked (a real 19 GB / 14 GB transformer weight file, no top-level `model.safetensors.index.json`). Direct T2I: FP8 sha256 `368ae934d5…` (691,968 bytes, 480×480 PNG, `run_at` 2026-07-09T12:16:10Z), NVFP4 sha256 `dca897ee83…` (same dims/size, 12:18:09Z). Full-stack T2I: FP8 job `d1f2b832…` succeeded, artifact sha256 `5ece5ec577…` (12:19:12Z); NVFP4 job `ce9bb9ed…` succeeded, artifact sha256 `b999233fce…` (12:20:13Z). Every artifact independently passes the image validity/dimension check; hashes are **not** byte-identical across direct vs. full-stack or across independent re-runs of the same checkpoint+prompt+seed (confirmed by re-running direct-FP8 twice: `5ece5ec577…` then `368ae934d5…`) — generation is not perfectly bit-deterministic run to run (GPU kernel execution-order variance), so hash equality/inequality between runs is not itself evidence of anything; validity is the property that matters and is checked per-artifact. All four artifacts share the identical `691,968`-byte size despite four distinct 64-character hashes — verified this is not a duplicate-content bug (the full hashes are pairwise distinct, not merely differing in the truncated form shown here) but is consistent with a fixed-size (uncompressed/stored) PNG encoding: `480×480×3` raw RGB bytes is `691,200`, leaving exactly `768` bytes of header/chunk overhead, matching all four. Hardware: RTX 5090, driver `610.43.02` / CUDA `13.3`; vLLM-Omni commit `697035018b70cef76b974a909d23371a9984c3f2`; prompt "a red apple on a wooden table, studio lighting", seed 42, 480×480, 35 steps, guidance 6.0. Guardrails off (`--no-guardrails`, the same runtime Compose `command:` override GPU-S1 used — never baked into `deploy/vllm-omni.Dockerfile`, R-10 stays open). Full-stack auth used a session-defined test `X-API-Key`, injected via an explicit env override — not `.env`'s own `COSMOS3_API_KEY` line, which is genuinely empty but whose trailing inline comment both a naive parser and Docker Compose's own `.env` parsing misread as if it were the value. | `GPU-S3` execution, 2026-07-09 (`docs/session_3/probes/evidence.json`, each fragment carries its own `run_at`) | High | Closes `GATE-GPU-S3-VALIDATION`'s T2I requirement and archived `R-13`'s "proxy image, not pinned-commit build" caveat. README/`model_setup.md`/`release_checklist.md` per-mode markings upgraded to "T2I-verified" for FP8/NVFP4 on this evidence. **Caught and fixed by sharded review (correctness axis):** the first pass of every generation probe silently mounted `.env`'s pinned `/data/models/Cosmos3-Nano-*-Blockwise` (a stale, pre-fix, manually LFS-patched local copy) instead of this fresh download, because that pin outranks the compose file's own repo-relative default. Fixed with an explicit env override plus a preflight that fails loudly on any mount mismatch (`docs/session_3/probes/compose_lifecycle.py`) — but that override lives only in this session's own probe scripts; the contract's own literal `deterministic_checks` command, run bare, still resolves to the stale directory (see `R-11`, newly opened, not fixable within this session's blast radius). Every generation task above was re-run against the corrected mount, and each fragment's `run_at` timestamp (added after a fresh-context adversarial verifier correctly pointed out that identical re-run output is otherwise unprovable from git history alone) independently confirms the re-run happened after both the mount fix and the timestamp fix landed. (T1's own `run_at` reflects a re-verification pass against already-correctly-downloaded content, not a second full transfer — `hf download`'s resume-aware behavior makes this a legitimate re-check, not a re-run in the same sense as T3–T7. The actual transfer completion times differ per checkpoint, not a single uniform figure: FP8's transformer weight file's mtime is `03:32:04Z`, ~8h42m before its `run_at`; NVFP4's is `09:42:24Z`, ~2h32m before its `run_at`.) |
| A best-effort, direct-only T2V smoke (256×256, 9 frames, 8 fps, NVFP4) generates a valid video artifact — the PRD FR-6 SHOULD deliverable. | NVFP4 succeeded on the first attempt (no FP8 fallback needed): job completed, artifact sha256 `71a79b6941…`, independently verified via `file`/`ffprobe` as a real ISO-media MP4 container, h264, 256×256, 8 fps, exactly 9 frames, `run_at` 2026-07-09T12:21:14Z (after the mount fix, provably — see the row above). Same hardware/commit/guardrails-off context as the T2I row above. | `GPU-S3` execution, 2026-07-09 (`docs/session_3/probes/evidence_t2v_smoke.json`) | High | `t2v_audio`/`i2v`/`forward_dynamics`/`reasoning`/720p video remain out of scope (PRD Non-Goals / VRAM). This one small NVFP4 clip does not itself upgrade any per-mode marking beyond the T2I row above — see `docs/model_setup.md` for the residual per-mode limits. |
