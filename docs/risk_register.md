# Risk Register - All Modes, GPU-Verified, Default-On

Date: 2026-07-24

Status values are blueprint-time. Sessions update rows with evidence as they
close. Where a row advances or defers an archived risk, the archived ID is noted;
see `docs/archive/phase-{1,2,3,4}/risk_register.md` for that history.

Probability/Impact are Low/Medium/High.

| ID | Risk | Probability | Impact | Owner Session | Mitigation / Gate | Status |
|---|---|---:|---:|---|---|---|
| R-01 | **Zero-BF16 action is blocked by the checkpoint.** The quantized `*-Blockwise` checkpoints ship **without** action tensors; the graft reads bf16 `action_*` adapters from the BF16 base and raises `FileNotFoundError` if absent (E-06). So "action off quantized-only weights" is impossible until the adapters are bundled into the checkpoint. | High | High | AM-S1, AM-S3 | Scope zero-BF16 action as a **checkpoint-packaging** task: bundle the small bf16 `action_*` adapters (E-17) into the quantized export via `tools/checkpoint_prep/` (E-13), pin a new immutable revision, and re-verify `t2i` against it (NFR-5). `AM-S1` confirms the gap and picks the path; `AM-S3` executes. If bundling proves infeasible, keeping the base's action tensors is an explicit owner decision (INV-4), not a silent BF16 default. | **Resolved (AM-S1)** — no longer a blocker: the quantized FP8/NVFP4 checkpoints **already bundle** the 5 BF16 `action_*` adapters (prior P6-S5 `checkpoint_prep mutate`; E-06 refuted). Zero-BF16 action is done at the checkpoint level; residual = serving-code reconciliation (drop the base graft) in AM-S3. |
| R-02 | **Option (a) may not serve action.** `vllm-omni` is an OpenAI server (chat is natural), but robot action is not a standard OpenAI surface; assuming `vllm-omni` serves action could waste a session. | Medium | High | AM-S1, AM-S3 | `AM-S1` tests action serving **separately** from reasoning; the side-by-side `(c)` backend (the existing diffusers action graft as its own residency plane) is the pre-authorized fallback (S-C). The contract never assumes (a) for action (project_contract §two-pass item 3). | **AM-S1:** `(a)` surface EXISTS (`/v1/realtime/robot/openpi`, WS 101) but is **unwired** ("robot policy not available"); `(c)` in-process graft is **broken** (key collision — checkpoint already has the tensors). Neither functional as-served → AM-S3 serving-path work; **prefer `(a)`**; `(c)` fallback retained. |
| R-03 | **Quantized reasoning quality is unacceptable** — especially NVFP4 4-bit — so the owner rejects it and the zero-BF16 reasoning goal stalls. | Medium | High | AM-S2, AM-S5 | The owner's manual quality gate (Decision 6) is the arbiter, run per format. FP8 (mild) is proven first; NVFP4 (4-bit) is the riskier case, isolated to `AM-S5`. A format that fails quality is an explicit owner decision (accept, use `(c)`, or document the limitation, INV-7), not a hidden downgrade. | Open (blueprint) |
| R-04 | **NVFP4 4-bit kernels don't support the reasoning/action inference path** on Blackwell (sm_120). NVFP4 `t2i` works via a Marlin FP4 kernel that repacks weights on CUDA after load and deliberately forbids layer-wise offload (`deploy/docker-compose.nvfp4.yml:15-20`); the LM/action paths are unproven. | Medium | High | AM-S5 | Prove the whole vertical on FP8 first (Decision 7), so NVFP4 is the only new variable at `AM-S5`. Attempt `(c)` per mode; a mode that cannot run on NVFP4 off quantized weights is a documented, owner-decided limitation for that format (INV-7), and does not block the FP8 stack. | Open (blueprint) |
| R-05 | **`t2i` regression.** Adding reasoning/action to the `vllm-omni` container, changing the engine wiring, or re-exporting the checkpoint can regress the one already-verified path. | Medium | High | AM-S2..S5 | `INV-2` + a mandatory `t2i` end-to-end smoke (FP8, and NVFP4 from `AM-S5`) as a blocking gate on every serving-path session (NFR-3); a re-exported checkpoint is `integrity_probe`d and `t2i`-re-verified at its new revision (R-10). | Open (blueprint) |
| R-06 | **Over-claim / honesty regression.** The deliverable is to promote modes to "GPU-verified"; a session could mark a mode verified after a single run or without a quality check — the exact false-claim class the phase-3 docs review caught. | Medium | Medium | AM-S2..S6 | `INV-6`: verified = recorded run **and** owner quality PASS, per format/mode. `AM-S6` carries a mandatory adversarial no-over-claim honesty pass (routing §5). README status is asserted a subset of what passed (deterministic check). | Open (blueprint) |
| R-07 | **Default-on before verified.** Merging the stacks and enabling modes by default before they run ships broken defaults (a `make up-fp8` that errors on `/v1/reason`). | Medium | High | AM-S4 | Sequencing: enable+verify each mode (`AM-S2`/`AM-S3`) **before** the default-on merge (`AM-S4`); `INV-7` gates default-on on the mode having passed that stack's gate. `AM-S4` runs a full all-modes smoke before closing. | Open (blueprint) |
| R-08 | **Co-residency / VRAM assumption drift.** `coresidency.py`'s budget math assumes a ~26 GiB BF16 reasoner (E-08). Zero-BF16 makes the reasoner ~quantized, and option (a) could merge Studio+Reasoning into one resident model (no swap). Leaving the stale assumption misleads; over-claiming co-residency risks OOM. | Low | Medium | AM-S2, AM-S4 | `INV-5`: preserve the evict-before-load safety net; adopt any co-residency/plane-merge only with a recorded OOM-free VRAM trace (`handoff_ok`/`within_budget` in `coresidency.py`). Update the coresidency contract's documented footprints to reflect quantized reasoning when that path is taken. | **AM-S1:** reasoning is **not** served by the omni model (chat = image-gen) → it stays a **separate plane, swap retained**; E-08's ~26 GiB BF16-reasoner math **still holds today**. The genuine merge candidate is **Studio+Action** (same omni model) — projected, VRAM-unmeasured for the wired policy; verify in AM-S3. |
| R-09 | **License premise inversion.** The owner says the BF16 base is `openmdw-1.0`; the repo's authoritative doc + README say `other` (E-14). Encoding either as fact risks a wrong license claim. | Low | Medium | AM-S6 | Treat as an **unresolved conflict** in `docs/evidence_map.md`; moot if zero-BF16 succeeds; load-bearing only if BF16 is reintroduced. `AM-S6` reconciles against the HF repo's own license page before any doc states it as fact. | Open (blueprint) |
| R-10 | **Checkpoint re-export drift.** Bundling action adapters mutates a `*-Blockwise` checkpoint; a bad rewrite could corrupt weights, break `t2i`, or drift from the public pinned revision. | Medium | High | AM-S3 | Use `tools/checkpoint_prep/` with its `integrity_probe` + tests (`tests/checkpoint_prep/**`); pin a **new** immutable revision (never mutate in place / never repoint `main`); keep weights external (NFR-1); re-verify `t2i` at the new revision (INV-2). Record the revision in `docs/model_setup.md` (NFR-5). | Open (blueprint) |
| R-11 | **Dead-code hazard.** If action moves to `vllm-omni` (option a), the dormant `diffusers` `gen_worker` action graft becomes dead code; removing it could break existing CPU tests (`test_action_*`, `test_gen_worker_mapping`, etc.). | Low | Low | AM-S3, AM-S4 | Prefer leaving the dormant path in place unless it actively conflicts; if removed, update the tests that exercise it rather than reverting the enablement. Not a required deletion. | Open (blueprint) |
| R-12 | **Scope creep into the WebUI.** The tabs already exist and are wired (E-12); a session could drift into unrequested UI work. | Low | Low | all | WebUI is out of scope (PRD §6); a session touches `webui/**` only if its own GPU verification surfaces a real UI bug against the now-working endpoint, and records why. | Open (blueprint) |
| R-13 | **Docs written before verification.** `docs/walkthrough.md` "expected outputs" / screenshots produced before the modes run (and pass quality) would be fiction. | Medium | Medium | AM-S6 | `AM-S6` runs **last**; example inputs may be drafted, but expected-output prose and image placeholders are finalized against real, owner-approved runs; the owner fills the images (INV-1, PRD Decision 8). | Open (blueprint) |
| R-14 | **Blast-radius bleed across enablement sessions.** `AM-S2`..`AM-S4` all touch the serving/orchestration seam (`api/app/main.py`, `deploy/**`); overlapping edits could contend or regress each other. | Medium | Medium | AM-S2..S4 | Each session contract fixes a narrow `allowed_files`; the sequence is strict (S2 → S3 → S4); each re-runs the CPU suite + `t2i` smoke before closing so a later session inherits a green baseline. Shared-surface files are called out in each contract's `failure_modes_to_watch`. | Open (blueprint) |
| R-15 | **Zero-BF16 reasoning has no proven serving path.** `vllm serve --omni` binds `/v1/chat/completions` to **image generation**, not text; the quantized understanding/text tower is not exposed as text by the omni serving (AM-S1 P3). So zero-BF16 reasoning (PRD Decision 3, INV-4) is **unproven** — reasoning currently needs the BF16-base side-car (E-04). | Medium | High | AM-S2 | AM-S2 must either (i) elicit text from the omni/understanding tower (a fork modality flag, or serve the quantized text tower on a separate engine) — unproven; or (ii) record an **explicit owner decision** to keep a BF16 reasoner side-car (an INV-4 exception documented at the gate, never silent). Not promotable to a MUST until proven. | **Resolved (AM-S2)** — zero-BF16 FP8 reasoning **PROVEN** on the 5090 (owner quality PASS). A small `fengwang/vllm` fork adds `--quantization fp8_blockwise_w8a16` (applies the existing vllm-omni `Fp8BlockwiseW8A16LinearMethod` to the LM MLP + a `cosmos3.py` scale-mapper fix), so a **non-omni** `vllm serve` decodes coherent text off the quantized understanding tower — no BF16, no side-car. Served as a separate `vllm-reasoner` container plane (evict-before-load, INV-5). Path (i) achieved; option (ii) not needed. **NVFP4 reasoning remains open → AM-S5 (S-D).** Evidence: `docs/session_2/evidence/P1–P3`. |
