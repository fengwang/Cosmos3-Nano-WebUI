# Risk Register - UX Simplification and Trusted-LAN Appliance Posture

Date: 2026-07-15

Status values are blueprint-time. Sessions update rows with evidence as they
close. Where a row advances or defers an archived risk, the archived ID is
noted; see `docs/archive/phase-1/risk_register.md` and
`docs/archive/phase-2/risk_register.md` for that history.

| ID | Risk | Probability | Impact | Owner Session | Mitigation / Gate | Status |
|---|---|---:|---:|---|---|---|
| R-01 | Removing API-key auth leaves the API — which mounts the root-equivalent host Docker socket — with **no application-layer access control**. If an operator sets `BIND_ADDR=0.0.0.0`, any LAN host can drive the socket-backed controller. | Medium | High | UX-S1 | Keep the loopback bind **default** (auth removal does not touch `BIND_ADDR`); make LAN exposure an explicit, documented opt-in; keep a slim honest note in `README.md`/`SECURITY.md` that there is no auth, the deployment assumes a trusted LAN, and the socket is root-equivalent. Carries forward archived `R-16` (socket hardening stays deferred, now documented as the *only* access control besides network placement). Mandatory pre-merge human gate. | Open (blueprint). |
| R-02 | After auth removal, a dangling reference (a test, an env var, an OpenAPI security scheme, a doc line, a compose passthrough) is left behind, breaking the build or misleading operators. | Medium | Medium | UX-S1 | Whole-repo `rg --hidden` sweep for `COSMOS3_API_KEY\|X-API-Key\|x_api_key\|api_key\|apiKey\|require_api_key\|UnauthorizedError` → zero matches outside `docs/archive/**` and this pack's own descriptive text; regenerate `schemas/openapi.json` and diff (no `x-api-key`); run the full CPU + WebUI suites. Learn from archived phase-2 `R-03`: `rg` skips dotfiles by default, so the sweep MUST use `--hidden` (`.env`, `.env.example`). | Open (blueprint). |
| UX-S1-A1 | Execution amendment: the blast radius and evidence_map E-14 enumerated the auth-coupled tests but omitted `tests/api/test_errors.py`, which imports `UnauthorizedError` and asserts the 401 mapping — deleting the symbol breaks it at import. Separately, the R-02 sweep is non-zero after removal because it descends into `.git/` (immutable commit history) and hits one legitimate `apiKey` mention in the self-excluded scanner `tests/test_private_ref_scan.py:12`. | High | Low | UX-S1 | Add `tests/api/test_errors.py` to the allowed blast radius (drop the import + `test_unauthorized_maps_to_401`). Refine the R-02 sweep with `-g '!.git'`; record the single `tests/test_private_ref_scan.py:12` `apiKey` mention as an allowed legitimate reference (the scanner documents why such words occur and self-excludes from its own scan). `.env` is gitignored/untracked → no committed change there. Owner-approved 2026-07-15 (Q1a). | Resolved in-session. |
| R-03 | The negative-prompt file is loaded from a hardcoded absolute path (`/data/models/Cosmos3-Nano/assets/negative_prompt.json`), violating INV-1 and breaking in-container (wrong mount), or the app crashes when the file is absent. | Medium | Medium | UX-S2 | Resolve the path from the existing configurable model-directory variable used by the serving container; if the file is missing or unreadable, log once and proceed with no negative prompt (graceful degradation), never crash generation. Deterministic check `EV-UX-NEGPROMPT-NO-ABS-PATH`. | Open (blueprint). |
| R-04 | `negative_prompt.json` is a nested structured object while the API field is a flat `str`; naive injection sends malformed data to the backend or silently degrades quality. | Medium | Medium | UX-S2 | `UX-S2` design MUST confirm the backend's accepted negative-prompt format (structured JSON vs serialized string) against the vLLM-Omni path before wiring; the recommended GPU smoke confirms the default negative prompt is actually received by the engine (server-log evidence), not merely set in the request. | Open (blueprint). |
| R-05 | 720p FP8 video sits near the 32 GB ceiling (bundled example peaked at 31,957/32,607 MiB, guardrails off, 189 frames). Guardrails-on, higher frame counts, or FP8 vs NVFP4 choice may OOM out of the box — the opposite of the intended UX win. | Medium | Medium | UX-S2 | The shipped default is 49 frames (lighter than the 189-frame proof); document the VRAM advisory (already present on the `hi-720` preset); recommend NVFP4 for 720p headroom; the `UX-S2` 5090 smoke (FR-6) MUST confirm the shipped 720p config fits for FP8 **and** NVFP4 at the shipped frame count and record the guardrails posture. Reconciles the archived "720p video > 32 GB" caveat (BF16 path) with the quantized path (see `docs/evidence_map.md` E-08). | Open (blueprint). |
| R-06 | Flipping the video resolution default silently also changes the text→image default, or the WebUI default preset and the API server default diverge, so "720p by default" is only half-true. | Medium | Medium | UX-S2 | Implement mode-aware defaulting: video modes default to 1280×720; `t2i` default is unchanged. The API server default and the WebUI default-selected preset MUST agree for video. Adversarial cases cover both divergences; deterministic check `EV-UX-RESOLUTION-DEFAULT-VIDEO-720`. | Open (blueprint). |
| R-07 | Gallery removal leaves a dead `/gallery` link or route, or an empty landing page (the home page's only content is the gallery link). | Low | Low | UX-S3 | Remove all three references (route dir, nav item, home link) and redirect/`/`→Studio in the same change; `rg -i 'gallery\|/gallery'` clean in `webui/app`/`webui/components` apart from the unrelated `HistoryList` "history/gallery" wording; WebUI build/typecheck green. | Open (blueprint). |
| R-08 | The README rewrite drops a must-have setup step, or leaves a dangling internal link (`docs/release_checklist.md` is now archived; `R-16` lives only in the archived phase-1 register). | Low | Medium | UX-S4 | Keep the checkpoint-download + `make build`/`make up-fp8`/`make health` essentials; run a relative-link check over `README.md`/`SECURITY.md`/`CONTRIBUTING.md` so every link resolves; repoint or drop the `release_checklist.md` and `R-16` references. | Open (blueprint). |
| R-09 | `UX-S1` and `UX-S4` both edit `README.md`/`SECURITY.md`, producing a merge conflict or a contradictory intermediate state (e.g. S4 reintroduces auth prose S1 removed). | Low | Medium | UX-S1, UX-S4 | Sequencing: `UX-S4` runs last, on the settled post-`UX-S1` state; `UX-S1`'s doc edits are confined to auth-specific lines only (Change Control §6). `UX-S4` re-verifies no auth prose remains before rewriting. | Open (blueprint). |
| R-10 | Deleting the auth tests removes the *only* assertions that exercised the four protected routers, so a later regression (e.g. an accidental re-introduction of a gate, or a broken router include) goes uncaught. | Low | Medium | UX-S1 | Independent-test-writer routing: rewrite the affected tests to assert the **new** open contract (protected routers now return non-401 for the same requests; health/metrics unchanged), rather than only deleting the old 401 assertions. | Open (blueprint). |
| R-16 (archived) | Docker-socket privilege / non-loopback exposure hardening. | — | High | Deferred | Explicitly out of scope this phase (PRD §6); auth removal makes network placement the primary control, so `R-01` raises this risk's salience. Tracked here by reference to `docs/archive/phase-1/risk_register.md` R-16. | Deferred, documented (unchanged by this phase). |
