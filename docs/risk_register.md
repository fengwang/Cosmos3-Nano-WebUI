# Risk Register - GitHub Migration Public Beta

Date: 2026-07-06

Status values are blueprint-time. Sessions update rows with evidence as they
close.

| ID | Risk | Probability | Impact | Owner Session | Mitigation / Gate | Status |
|---|---|---:|---:|---|---|---|
| R-01 | Private hosts, private paths, codenames, or local-only artifact references leak into the public repo. | Medium | High | MIG-S1, MIG-S3, MIG-S7 | Define scrub rules in `MIG-S1`; run recursive `rg` scans in import and README sessions; block release at `GATE-MIG-S8-BETA` if any remain. | Open; `MIG-S2` and `MIG-S3` reviews caught and scrubbed private values from session docs (`MIG-S3` FA-6/R1). Imported source and `docs/session_3/**` private-reference scans are clean; `MIG-S4` `docs/session_4/**` + `docs/model_setup.md` scans clean. Note: `MIG-S4` drift D3 records that the **external** public HF repos ship dev-scratch (`_s2_*`) / provenance / loader-script files — recommend owner HF-side cleanup (contents not read here). `MIG-S7`/`MIG-S8` must re-scan. **`MIG-S7` re-scan (README, hygiene files, `.github/**`, `docs/session_7/**`) is clean — `tests/test_private_ref_scan.py` = 0 findings; the maintainer contact email in `SECURITY.md`/`CODE_OF_CONDUCT.md` is an intended public contact, not a private reference.** **`MIG-S8` re-scan (whole tracked tree + `docs/session_8/**`): clean** — committed `tests/test_private_ref_scan.py` = 0 findings, plus the broad-scan human gate (lockfile-URL credential / absolute-path / private-host scans) clean; the only `hexagon.com` hits are the intended public maintainer contact. Mitigated for beta; standing per-session vigilance item, not a release blocker. |
| R-02 | The Cosmos3 vLLM-Omni patch series conflicts with current GitHub fork `main` or upstream changes. | Medium | High | MIG-S2 | Use branch-and-compare rebase; run targeted vLLM-Omni tests; pin a public commit only after tests pass. | Closed for `MIG-S2`: branch/tag `cosmos3-nano-webui-mig-s2` resolve publicly to `697035018b70cef76b974a909d23371a9984c3f2`; expanded targeted tests passed. |
| R-03 | The public HF checkpoints differ from the runtime assumptions needed by the WebUI/API and vLLM-Omni fork. | Medium | High | MIG-S4 | Probe FP8/NVFP4 metadata, file layout, license, and compatibility before Docker and README depend on them. | Verified/routed for `MIG-S4`: revisions + license (`openmdw-1.0`) recorded; both self-contained. **Drift D1 (high):** in-process `diffusers_oracle` cannot load+verify either current public checkpoint as-is (FP8 recipe `fp8_blockwise_mixed` ≠ exact `"fp8"`; NVFP4 lacks `modelopt_state.pt`/`quantization_config.json`) — default `vllm_omni` container path routed to `MIG-S6`/`MIG-S8`. **D2 (low):** BF16 base is public at `nvidia/Cosmos3-Nano` (not `wfen/Cosmos3-Nano`). See `docs/session_4/drift_report.md`. |
| R-04 | The NVFP4 HF model card is empty, so public users lack setup context. | High | Medium | MIG-S4, MIG-S7 | Put checkpoint setup in this repo's README/docs; optionally create a separate model-card follow-up outside this repo. | Updated for `MIG-S4`: NVFP4 card is a **62-byte stub** (drift D4), not fully empty; compensated by `docs/model_setup.md` (repo IDs, revisions, env, mount, per-mode matrix). Populating the HF card is an external follow-up; README (`MIG-S7`) must not rely on the HF card. |
| R-05 | CPU-only CI passes while GPU inference is broken. | Medium | High | MIG-S5, MIG-S8 | Keep CPU CI as fast gate, but require manual GPU release evidence before beta GO. | Structurally mitigated for `MIG-S5` (**still open**): `.github/workflows/ci.yml` is explicitly CPU-only (no CUDA/weights/secrets); `gpu` marker + `tests/conftest.py` guard + `-m "not gpu"` keep GPU tests out of CPU CI; manual gate `COSMOS3_ENABLE_GPU_TESTS=1 pytest -m gpu` documented (`docs/session_5/local_checks.md`). GPU inference remains unproven by CI — evidence required before beta GO (`MIG-S8`). **`MIG-S8` owner disposition:** GPU is deferred to a post-publish GPU session; the full inference surface ships **beta-limited** (each mode marked GPU-unverified in the README, `INV-8`). The R-05 residual (GPU unproven by CI) is **accepted for the beta / research-preview posture** by owner decision and routed to the GPU session — owned, not an unowned release blocker. |
| R-06 | Docker examples accidentally bake or download weights into images. | Low | High | MIG-S6 | Use external mounts/configurable paths; scan Dockerfiles and Compose for weight-copy patterns; document explicit setup. | Mitigated for `MIG-S6`: `.dockerignore` excludes `models/` + weight/media globs; all Dockerfiles use narrow COPYs (no `COPY . .`); the weight-copy scan (`COPY/ADD *.safetensors\|pt\|pth\|ckpt` over `deploy/`) is clean; checkpoints are external `:ro` bind-mounts with repo-relative `../models/<Repo>` defaults (INV-2); no runtime auto-download. |
| R-07 | Legacy plain vLLM or TensorRT-LLM code is imported and bloats the repo or creates broken optional paths. | Medium | Medium | MIG-S3 | Exclude legacy submodules and unsupported code unless a public runtime dependency is proven. | Mitigated for `MIG-S3`: `submodules/` and `api/engines/trtllm/` excluded; torch-free `import app.main` verified without legacy; no `trtllm`/`tensorrt` reference remains in `api/tools/schemas`. |
| R-08 | The full target product surface is too broad for one public beta migration. | Medium | Medium | MIG-S3, MIG-S8 | Preserve the full target surface, but allow `MIG-S8` to mark specific modes beta-limited if manual evidence is missing. | **`MIG-S8`: mitigation exercised (owner-dispositioned)** — all GPU inference modes (generation/reasoning/action) are marked **beta-limited**; the CPU-tested surface (jobs/SSE, health, metrics, artifacts wiring, WebUI) stays in scope. The full surface is preserved in code; unverified modes are flagged. Not an open unowned blocker. |
| R-09 | README claims outrun evidence, especially around RTX 5090, FP8, NVFP4, and performance. | Medium | High | MIG-S7, MIG-S8 | Use evidence-qualified wording; link claims to evidence rows; adversarial review README before beta. | Mitigated for `MIG-S7`: the README carries a beta / research-preview banner up front, marks every generation/reasoning/action mode GPU-unverified (`MIG-S8` gate), makes no production/performance claim, and links `docs/evidence_map.md`; forbidden-claims review is clean over the deliverables and the fresh-context verifier exercised the "GPU shipped without evidence" case. Residual: GPU/performance evidence itself is the `MIG-S8` gate. |
| R-10 | GitHub Actions fail due to dependency drift or missing generated files. | Medium | Medium | MIG-S5 | Pin setup versions, use lockfiles, add schema sync checks, and classify failures before changing source. | Mitigated for `MIG-S5`: CI uses `uv sync --frozen` + `pnpm install --frozen-lockfile` and pinned action major tags; two schema-sync gates (`tests/test_openapi.py` app→openapi + `pnpm gen:api` diff openapi→types), both proven to fail on injected drift (adversarial verification). One SPEC_GAP classified + fixed (FA-2: `pnpm build` must precede `typecheck`). |
| R-11 | License boundaries are unclear between MIT repo code, model license, and dependency licenses. | Low | High | MIG-S7 | Add MIT `LICENSE`; README calls out HF model license and dependency license responsibility. | Mitigated for `MIG-S7`: `LICENSE` (MIT) is scoped to repo code with an explicit note that it does not cover the model weights; the README states the three-way separation (repo MIT vs weights `openmdw-1.0` vs base `other`) in both the checkpoint-setup and license sections; dependency licenses are acknowledged (`docs/model_setup.md`). |
| R-12 | Public history becomes noisy if private development history is imported directly. | Low | Medium | MIG-S3 | Use fresh curated public commits and summarize provenance in public evidence terms only. | Mitigated for `MIG-S3`: curated allowlist copy with fresh per-area commits on `session-3`; no private git history imported. |
| R-13 | Docker local build fails because the pinned vLLM-Omni fork commit is not installable from public inputs. | Medium | High | MIG-S2, MIG-S6, MIG-S8 | `MIG-S2` proves fork install/test; `MIG-S6` builds from the pinned commit and records failure class if it fails. | Advanced by `MIG-S6`: `deploy/vllm-omni.Dockerfile` installs the fork by **immutable commit** `697035018b70…` over public HTTPS (INV-3); the heavy/GPU vLLM-Omni image build + a clean `pip install` from the pin are **deferred to the `MIG-S8` gate** (commands recorded in handoff). The api (lean) + webui images build from public inputs and pass here. **`MIG-S8` owner disposition:** the vLLM-Omni image build + a clean `pip install` from the pin are **deferred to the post-publish GPU session** (owner decision 1); the exact commands are recorded in `docs/handoff.md` + `docs/release_checklist.md` §6. Not a beta blocker — cloning/reading the public repo and the CPU CI do not require the GPU image. Owned/routed. |
| R-14 | A secret or token is added to workflow files or examples. | Low | High | MIG-S5, MIG-S7 | Keep first milestone free of registry publishing and secrets; scan `.github/**`, docs, and env examples. | Mitigated for `MIG-S5`: `ci.yml` declares `permissions: contents: read`, references no secret, uses no registry login or self-hosted runner; committed `tests/test_private_ref_scan.py` scans `.github`/`api`/`webui`/`tests`/`schemas`/`docs` for secret values (private-key headers, `hf_`/`sk-`/AWS/GitHub-PAT classes) + private paths + weight files, clean on the tree. Broad lexical + lockfile-URL scan remains an `MIG-S8` human gate; `MIG-S7` re-scan on README/hygiene. |
| R-15 | Public beta release lacks enough project hygiene for outside users to report bugs safely. | Medium | Medium | MIG-S7 | Add `SECURITY.md`, `CONTRIBUTING.md`, issue templates, and release checklist before beta GO. | Mitigated for `MIG-S7`: added `SECURITY.md` (private reporting; forbids public-issue disclosure), `CONTRIBUTING.md` (mirrors CI), `CODE_OF_CONDUCT.md`, `.github/ISSUE_TEMPLATE/{bug_report,feature_request,config}.yml` (`blank_issues_enabled:false`, security redirect, no sensitive-data fields), `.github/PULL_REQUEST_TEMPLATE.md`, and `docs/release_checklist.md`. |
| R-16 | The api container mounts the host Docker socket (root-equivalent) to drive the generation container. | Medium | High | MIG-S6, MIG-S7, MIG-S8 | Confine access to a fixed-verb / operator-config-name controller (`DockerCliController`); publish ports to loopback (`BIND_ADDR=127.0.0.1`) by default; evaluate a `docker-socket-proxy` and require the API key before any non-loopback exposure. | Open for `MIG-S6`: privilege is **confined + documented** (design D-6 — fixed `start`/`stop`/`inspect` argv, operator-config name, no shell) and ports default to loopback; `docker-socket-proxy` hardening + release review deferred to `MIG-S8`. **`MIG-S7` update:** X-1 is fixed — the WebUI BFF now forwards `COSMOS3_API_KEY` as `X-API-Key`, so requiring the key before any non-loopback exposure is enforceable end to end (previously the key 401'd the WebUI→API proxy). `SECURITY.md`/README document the loopback-by-default + auth posture. The `docker-socket-proxy` and the non-loopback exposure policy remain the `MIG-S8` gate. Note: `api/orchestrator/container.py` cites a stale `R-15` (the original private project's numbering) for this privilege — this row (R-16) is the migration's tracker. **`MIG-S8` owner disposition:** for the beta, the confined fixed-verb controller + loopback-by-default binding + `X-API-Key` (enforceable end-to-end after the S7 X-1 fix) are **accepted**; the `docker-socket-proxy` evaluation + the non-loopback exposure policy are a **post-beta hardening** item (`docs/release_checklist.md` §8). Documented for operators in `SECURITY.md`/README. Owned/routed — not an open unowned blocker. |
