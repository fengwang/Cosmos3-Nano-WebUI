# Risk Register - Single-GPU Comfort and ADHD-Friendly Onboarding

Date: 2026-07-24

Status values are blueprint-time. Sessions update rows with evidence as they
close. Where a row advances or defers an archived risk, the archived ID is
noted; see `docs/archive/phase-{1,2,3}/risk_register.md` for that history.

| ID | Risk | Probability | Impact | Owner Session | Mitigation / Gate | Status |
|---|---|---:|---:|---|---|---|
| R-01 | **Wrong timeout / unserved goal.** The request names a "video-generation timeout"; taken literally, a session raises a generation cap that does not exist at 10 min (generation is already 40 min–2 h; cold start 30 min), so the actual pain — a cold reload after a >10-min pause — is left unfixed. | Medium | Medium | LX-S1 | Scope the change to `COSMOS3_IDLE_TIMEOUT_SECONDS` (idle keep-warm), the only 10-min default (E-01/E-02); correct the framing in PRD §1.1; record the generation/cold-start audit (E-03/E-04, FR-4) so the reviewer can confirm the change serves the goal. | Open (blueprint). |
| R-02 | **Idle-default drift.** The idle default lives in two places — the `main.py` env fallback and the `Orchestrator` constructor default (E-06). Changing only one leaves a directly-constructed orchestrator (tests, future callers) silently at 10 min, so "the default is 30 min" is only half-true. | Medium | Low | LX-S1 | Change both `api/app/main.py:173` (`"600"`→`"1800"`) and `api/orchestrator/manager.py:46` (`600.0`→`1800.0`); add a CPU unit test asserting the app wires 1800 with no env, honors an override, and treats `0` as disabled (FR-1/FR-2, `EV-LX-IDLE-DEFAULT-1800`). | Open (blueprint). |
| R-03 | **Longer keep-warm holds VRAM.** After a job, the resident plane now occupies VRAM for 30 min instead of 10; on a shared box this could look like a leak or delay another workload, and a separate concern — the 30-s WebUI SSE heartbeat — could still drop a genuinely-quiet long job in the browser (E-08). | Low | Low | LX-S1 | On the single-user 5090 posture this is the intended behavior; `acquire` evicts-before-load and cancels the idle timer, so another residency still preempts immediately — no starvation (INV-4, E-05). Document the new behavior in the README status callout via `LX-S2`. The SSE heartbeat is explicitly out of scope and recorded as a future consideration, not changed. | Open (blueprint). |
| R-04 | **README honesty regression under a punchy hook.** The owner-chosen punchier hook with caveats moved to the bottom invites over-claiming (a hook or benefit line implying every mode is GPU-verified) or a quietly dropped/softened caveat — the same false-claim class the phase-3 docs review caught. | Medium | Medium | LX-S2 | Honesty invariant INV-6: the hook must be factually true; the per-mode verification status stays (only t2i "GPU-verified end to end"); the full caveats stay **visible** (never inside `<details>`), relocated to the end; every "why" traces to an evidence row. Mandatory adversarial no-over-claim / no-lost-caveat pass despite low risk (routing §5). Deterministic `EV-LX-README-HONEST-CAVEATS`, `EV-LX-README-VERIFIED-SUBSET`. | Open (blueprint). |
| R-05 | **S1/S2 shared README surface.** Both a timeout doc-update and the README rewrite could touch `README.md` (it references a timeout at `:136`), producing a contradictory intermediate state or a conflict (phase-3's `R-09`). | Low | Low | LX-S1, LX-S2 | Sequencing: `LX-S1` does **not** edit `README.md`; `LX-S2` runs last and owns all README prose, documenting the settled 30-min behavior (Change Control §6, Hard Commitment 6). | Open (blueprint). |
| R-06 | **Codespaces recommendation shipped.** Following the Gemini report's remote-development pillar (E-12) would add a "run it in the cloud / open in Codespaces" path that cannot work — Codespaces have no suitable GPU — misleading users and wasting their time. | Medium | Medium | LX-S2 | Not adopted: the disagreement is stated in the evidence map (E-12) and the README carries no cloud/Codespaces CTA (FR-9, INV-7). Deterministic sweep `EV-LX-DOCS-LINKS-RESOLVE` also asserts no such CTA/link. | Open (blueprint). |
| R-07 | **Over-specified README contract.** Mandating exact headings, section order, or word counts (the reports give specific but conflicting ones, E-11/E-16) would make the contract brittle and partly untestable, and would over-constrain the writing session. | Low | Low | LX-S2 | Fix testable **properties**, not prose: what/features precede deep install; a runnable TL;DR near the top; a Mermaid map present; `<details>` present for verbose content; caveats present, visible, at the end; links resolve. Leave copy and exact headings to the session (`EV-LX-README-STRUCTURE`). | Open (blueprint). |
| R-08 | **Broken or dropped references after the rewrite.** A restructure drops a must-have setup step, or leaves a dangling internal link/anchor (mirrors phase-3's `R-08`). | Low | Medium | LX-S2 | Preserve the runnable quickstart essentials (clone → pinned `hf download` → `make build` → `make up-fp8` → `make health` → Studio), each verified against the `Makefile` (INV-5); run a relative-link + in-page-anchor resolver over `README.md` and any doc it links that the session edits (`EV-LX-DOCS-LINKS-RESOLVE`, `EV-LX-README-RUNNABLE-QUICKSTART`). | Open (blueprint). |
| R-09 | **Collapsible / progressive-disclosure rendering pitfalls.** A `<details>` block without the required blank line after `</summary>` renders broken; content hidden by default can also be missed on mobile or by search — dangerous if anything essential is inside. | Low | Low | LX-S2 | Keep only verbose/advanced/troubleshooting content in `<details>` (never essential setup or caveats, INV-6/E-13); follow the blank-line-after-`</summary>` rule; verify the rendered README (owner read recommended before merge). | Open (blueprint). |
| R-10 | **Mermaid map adds load or drifts.** An over-large diagram (too many nodes) increases cognitive load — the opposite of the goal — or the diagram drifts from the real component flow and misleads. | Low | Low | LX-S2 | Keep the map ≤7 nodes (E-17) and faithful to the real WebUI → API → generation-container flow (INV-8); because it is text it is maintainable in-repo; a reviewer checks it against `deploy/**` + `api/orchestrator/**` reality. | Open (blueprint). |
